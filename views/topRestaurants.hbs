<ul class="nav nav-tabs">
  <li class="nav-item">
    <a class="nav-link" href="/restaurants">首頁</a>
  </li>
  <li class="nav-item">
    <a class="nav-link" href="/restaurants/feeds">最新動態</a>
  </li>
  <li class="nav-item">
    <a class="nav-link active" href="/restaurants/top">TOP10 人氣餐廳</a>
  </li>
  <li class="nav-item">
    <a class="nav-link" href="/users/top">美食達人</a>
  </li>
</ul>
<br /><br />
<h1>美食達人</h1>
<hr />
{{#each restaurants}}
  <div class="card mb-3 mx-auto">
    <div class="row no-gutters">
      <div class="col-md-4">
        <a href="/restaurants/{{id}}" class="rest-link">
          <div class="rest-img" style="background-image: url({{image}}), url('/img/loading.gif');"></div>
          <div class="overlay"></div>
        </a>
      </div>
      <div class="col-md-8">
        <div class="card-body">
          <a href="/restaurants/{{id}}">
            <h4 class="card-title">{{name}}</h4>
          </a>
          <span class="badge badge-secondary">收藏數：{{dataValues.favUserCount}}</span>
          <p class="card-text line-camp">{{description}}</p>
          <a href="/restaurants/{{id}}" class="btn btn-primary">Show</a>
          {{#if isFavorite}}
            <form action="/favorite/{{id}}?_method=DELETE" method="POST" class="d-inline-block">
              <button type="submit" class="btn btn-danger">移除最愛</button>
            </form>
          {{else}}
            <form action="/favorite/{{id}}" method="POST" class="d-inline-block">
              <button type="submit" class="btn btn-primary">加到最愛</button>
            </form>
          {{/if}}
        </div>
      </div>
    </div>
  </div>
{{/each}}